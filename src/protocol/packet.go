package protocol

import (
//	"bytes"
//	"encoding/binary"
)


/*

	alarm  message
	
*/


func Packet(message []byte) []byte {
	buf := make([]byte, 1024)

	return buf
}

func UnPacket(buf []byte) []byte {
	bufRet := make([]byte, 1024)

	return bufRet
}



